<Window x:Class="PonsCognitiveServicesWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        xmlns:helpers="clr-namespace:PonsCognitiveServicesWPF.Helpers"
        mc:Ignorable="d ignore"
        Height="300"
        Width="300"
        Title="PONS + Cognitie Services"
        DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Background="#007b30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Button>
                    MICRO
                </Button>
            </StackPanel>
            <TextBox Grid.Column="1" Text="{Binding Query, Mode=TwoWay}" Margin="5"/>
            <Button Grid.Column="2" Command="{Binding GetRestRequestCommand}" Margin="5">
                GO
            </Button>
        </Grid>

        <WebBrowser Grid.Row="1" helpers:BrowserBehavior.Html="{Binding WebAddress}"/>
    </Grid>
</Window>
